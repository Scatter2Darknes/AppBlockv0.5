<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppBlock"
        tools:targetApi="31">
        <meta-data
            android:name="android.allow_usage_access"
            android:value="true" />

        <activity
            android:name=".DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppBlock" />

        <activity
            android:name=".MonitoredAppsActivity"
            android:exported="false"
            android:theme="@style/Theme.AppBlock"
            />
<!--        <activity android:name=".BlockedAppsManager" android:exported="false"-->
<!--            tools:ignore="Instantiatable" />-->
        <activity android:name=".AppListActivity" android:exported="false" />
        <activity android:name=".AppAdapter" android:exported="false"
            tools:ignore="Instantiatable" />
        <activity android:name=".ExcludedApps" android:exported="false"
            tools:ignore="Instantiatable" />

    </application>
</manifest>